This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

# the news-test

## Built with react 

#### 

## Available Scripts
### A single page application which state is managed by react, every single changed is managed and renderd by react. this include `routing ` ~core pages~
#### the application is splitted into multiple core component, the idea is to keep the component small and manageable, 
#### the idea behinde the style is to ensure that it is resusable  while the other components has a lot of logic state management used in the appliction
#### wrote the 'REACT HOOKS'  from scratch and customizable .